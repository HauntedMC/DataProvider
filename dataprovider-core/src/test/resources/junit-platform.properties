# Testcontainers 1.21 uses JUnit's legacy CloseableResource, which JUnit 6 still closes correctly.
# Disable only the newer AutoCloseable store mode to avoid its compatibility warning during integration tests.
junit.jupiter.extensions.store.close.autocloseable.enabled = false
